<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>

    <style>
        @import url('https://fonts.googleapis.com/css2?family=Merriweather:wght@300;400;700&family=Inter:wght@300;400;500&display=swap');

        body {
            margin: 0;
            font-family: Inter, sans-serif;
            background: radial-gradient(circle at center,
                        #1b1a17 0%,
                        #14130f 50%,
                        #0d0c0a 100%);
            color: #e8ddc8;
        }

        .container {
            display: flex;
            height: 100vh;
        }

        /* SIDEBAR */
        .sidebar {
            width: 260px;
            background: rgba(20, 18, 15, 0.85);
            border-right: 1px solid rgba(212, 184, 109, 0.35);
            padding: 30px 20px;
            box-shadow: inset 0 0 18px rgba(0,0,0,0.45);
        }

        .sidebar h2 {
            margin: 0 0 25px;
            font-family: Merriweather, serif;
            font-size: 20px;
            letter-spacing: 1px;
            color: #d7c79c;
            text-align: center;
        }

        .nav-button {
            display: block;
            width: 100%;
            padding: 12px 14px;
            margin-bottom: 18px;
            text-decoration: none;

            background: rgba(30, 28, 24, 0.65);
            color: #e8ddc8;
            border-radius: 8px;
            border: 1px solid rgba(180, 160, 110, 0.25);

            font-size: 15px;
            transition: 0.25s background, 0.25s border-color;
        }

        .nav-button:hover {
            background: rgba(40, 37, 33, 0.9);
            border-color: rgba(219, 192, 125, 0.65);
        }


        /* MAIN SECTION */
        .main {
            flex: 1;
            padding: 40px 50px;
        }

        .main h1 {
            margin: 0;
            font-family: Merriweather, serif;
            font-size: 28px;
            color: #d7c79c;
            letter-spacing: 1px;
        }

        /* Cards */
        .cards {
            margin-top: 40px;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
            gap: 25px;
        }

        .card {
            background: rgba(22, 20, 17, 0.75);
            padding: 25px;
            border-radius: 12px;

            border: 1px solid rgba(212, 184, 109, 0.28);
            box-shadow:
                0 0 14px rgba(0,0,0,0.4),
                inset 0 0 12px rgba(0,0,0,0.25);

            transition: 0.25s transform, 0.25s border-color;
        }

        .card:hover {
            transform: translateY(-3px);
            border-color: rgba(219, 192, 125, 0.55);
        }

        .card-title {
            font-size: 15px;
            color: #cbbf9e;
            letter-spacing: 0.5px;
            font-family: Merriweather, serif;
        }

        .card-value {
            font-size: 38px;
            font-weight: 700;
            margin-top: 12px;
            color: #e8ddc8;
            font-family: Inter, sans-serif;
        }
    </style>
</head>
<body>

<div class="container">

    <!-- SIDEBAR -->
<!-- <div class="sidebar">
    <h2>Navigation</h2>

    <a class="nav-button" href="/admin/Departments/create">Create Departments</a>
    <a class="nav-button" href="/admin/Departments/list">Departments</a>
    <a class="nav-button" href="/admin/Users/create">Add User</a>
    <a class="nav-button" href="/admin/Users/list">User List</a>
</div> -->

<%- include('Navigation') %>

    <!-- MAIN -->
    <div class="main">
        <h1>Dashboard Overview</h1>

        <div class="cards">
            <div class="card">
                <div class="card-title">Total Departments</div>
                <div class="card-value">67</div>
            </div>

            <div class="card">
                <div class="card-title">Total Students</div>
                <div class="card-value">56</div>
            </div>

            <div class="card">
                <div class="card-title">Total Professors</div>
                <div class="card-value">69</div>
            </div>

            <div class="card">
                <div class="card-title">Total HODs</div>
                <div class="card-value">67</div>
            </div>
        </div>

    </div>

</div>

</body>
</html>

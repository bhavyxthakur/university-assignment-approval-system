<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot password</title>
 <style>
    body {
        margin: 0;
        padding: 40px;
        font-family: Inter, sans-serif;

        background: radial-gradient(circle at center,
                    #1b1a17 0%,
                    #14130f 50%,
                    #0d0c0a 100%);
        color: #e8ddc8;

        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
    }

    form {
        background: rgba(22, 20, 17, 0.78);
        padding: 32px;
        width: 360px;
        border-radius: 12px;

        border: 1px solid rgba(212, 184, 109, 0.35);
        box-shadow:
            0 0 18px rgba(0,0,0,0.45),
            inset 0 0 12px rgba(0,0,0,0.25);

        display: flex;
        flex-direction: column;
        gap: 18px;
    }

    input {
        width: 100%;
        padding: 12px;
        border-radius: 8px;

        background: rgba(30, 28, 24, 0.75);
        border: 1px solid rgba(180, 160, 110, 0.35);
        color: #e8ddc8;
        font-size: 15px;
        outline: none;

        transition: 0.25s border-color, 0.25s background, 0.2s box-shadow;
    }

    input::placeholder {
        color: #b3a78b;
        letter-spacing: 0.5px;
    }

    input:focus {
        border-color: rgba(219, 192, 125, 0.75);
        background: rgba(35, 33, 29, 0.85);
        box-shadow: 0 0 10px rgba(219, 192, 125, 0.25);
    }

    button {
        padding: 12px;
        border-radius: 8px;

        background: linear-gradient(
            90deg,
            rgba(180, 150, 85, 0.9),
            rgba(205, 178, 110, 0.9)
        );
        border: none;

        color: #1b1a17;
        font-weight: 600;
        font-size: 16px;
        letter-spacing: 0.8px;
        cursor: pointer;

        transition: 0.2s transform, 0.2s box-shadow;
    }

    button:hover {
        transform: translateY(-2px);
        box-shadow: 0 0 12px rgba(219, 192, 125, 0.35);
    }
</style>
</head>
<body>
    <form action="/admin/fp/otp" method="POST">
        <input type="text" placeholder="enter your email" name="email"/>
        <button>generate otp</button>
    </form>
</body>
</html>